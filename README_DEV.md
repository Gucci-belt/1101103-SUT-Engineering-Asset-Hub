# 🚧 Dev Branch (Local Use Only)

Branch นี้ (`dev`) ถูกตั้งค่าไว้สำหรับ **"รันบนเครื่องตัวเอง"** เท่านั้นครับ
โดยมีการปรับแต่งดังนี้:

1.  **ไม่ Deploy ขึ้น Server จริง:**
    *   ผมแก้ `Jenkinsfile` ให้มันข้ามขั้นตอน Deploy ไปแล้ว
    *   คุณสามารถ Push Code เข้ามาเก็บไว้ได้ โดยไม่ต้องกลัวว่าเว็บจริงจะพัง

2.  **โหมดแกล้งทำไม่เสร็จ (WIP Mode):**
    *   ปุ่ม **Admin** ถูกซ่อนไว้ (ใน `Navbar.jsx`)
    *   เหมาะสำหรับเอาไว้ Present งานให้อาจารย์ดูว่าเป็น "เวอร์ชันกำลังพัฒนา"

---

## 💻 วิธีรัน (Local Run)

1.  เข้าโฟลเดอร์ `client`
    ```bash
    cd client
    ```
2.  ติดตั้งและรัน
    ```bash
    npm install
    npm run dev
    ```
3.  เปิด Browser: `http://localhost:5173`

ขอให้สนุกกับการ Present ครับ! 🚀
