# 🚧 Developer Guide: Local Development & WIP Mode

คู่มือสำหรับนักพัฒนา (Developer) ที่ต้องการรันโปรเจกต์บนเครื่องตัวเอง (Localhost) หรือต้องการเปิดโหมด **"Work in Progress (WIP)"** สำหรับการนำเสนอความคืบหน้า

> **Note:** Branch `dev` ถูกออกแบบมาเพื่อการ Dev เท่านั้น จะไม่มีการ Deploy ขึ้น Server จริง

---

## 💻 1. การติดตั้งและรัน (Local Setup)

หากคุณต้องการรันแค่ Frontend และเชื่อมต่อกับ Backend จริง (หรือ Mock) ให้ทำตามขั้นตอนนี้:

### Prerequisites:
*   [Node.js](https://nodejs.org/) (v18+)

### Steps:
1.  **เข้าสู่โฟลเดอร์ Client:**
    ```bash
    cd client
    ```

2.  **ติดตั้ง Dependencies:**
    ```bash
    npm install
    ```

3.  **Start Development Server:**
    ```bash
    npm run dev
    ```
    *   Web App จะทำงานที่ `http://localhost:5173`
    *   ระบบจะเชื่อมต่อกับ API Server ตามที่ระบุใน `.env` (Default: Production API)

---

## 🎭 2. โหมดสาธิต (Demo / WIP Mode)

ใน Branch `dev` นี้ เราได้เปิด **Feature Toggle** บางอย่างไว้เพื่อให้เหมาะสมกับการนำเสนอความคืบหน้า (Present):

### ✅ Features ที่เปิดใช้งาน:
*   **User Login (Clerk):** ใช้งานได้จริง เชื่อมต่อกับ Production
*   **Borrow Asset:** ระบบยืม-คืน ใช้งานได้ปกติ
*   **Dashboard:** ดึงข้อมูล Real-time

### 🙈 Features ที่ถูกซ่อนไว้ (Under Construction):
*   **Admin Panel Switch:** ปุ่มสลับไปหน้า Admin ถูกซ่อนไว้ใน `Navbar.jsx`
    *   *Status:* To create "Under Development" impression.
*   **Deployment:** `Jenkinsfile` ถูกปิดการทำงานสำหรับ Branch นี้ (No Auto-Deploy)

---

## ⚠️ ข้อควรระวัง (Important)
*   User ที่สร้างใน Localhost จะเป็น User จริงบน Clerk (เพราะใช้ Production ID)
*   **ห้าม Push ขึ้น Main** จนกว่าจะต้องการ Deploy จริง

---
*Back to Main Documentation: [README.md](./README.md)*
